datastore: test
recreate: true
config:
  driverName: aerospike
  parameters:
    namespace: test
    host: 127.0.0.1
    dateFormat: yyyy-MM-dd hh:mm:ss
tables:
  - table: users
    pkColumns:
      - id